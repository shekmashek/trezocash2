/*
  # [Structural] Add description to projects
  [This migration adds a 'description' column to the 'projects' table to store a brief summary for each project.]

  ## Query Description: [This is a safe, non-destructive operation that adds a new text column to the projects table. Existing projects will have a NULL value for this new column, which is expected and handled by the application.]
  
  ## Metadata:
  - Schema-Category: "Structural"
  - Impact-Level: "Low"
  - Requires-Backup: false
  - Reversible: true
  
  ## Structure Details:
  - Table: public.projects
  - Column Added: description (text)
  
  ## Security Implications:
  - RLS Status: [Enabled]
  - Policy Changes: [No]
  - Auth Requirements: [None]
  
  ## Performance Impact:
  - Indexes: [None]
  - Triggers: [None]
  - Estimated Impact: [Negligible performance impact on existing queries.]
*/
ALTER TABLE public.projects
ADD COLUMN IF NOT EXISTS description text;
