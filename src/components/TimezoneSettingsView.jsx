import React from 'react';

const TimezoneSettingsView = () => {
  // This component is deprecated as timezone settings are now in Project Settings.
  return null;
};

export default TimezoneSettingsView;
